# Create a personal access token at:
# https://calendly.com/integrations/api_webhooks
# Grant only the scopes you need. The CLI's read-only commands need:
# users:read event_types:read availability:read locations:read
CALENDLY_TOKEN=

