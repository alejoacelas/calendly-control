<!--ai-->
# calendly

Calendly hosts the booking pages, availability checks, invitations, and Google Meet links. Agents change it through the official API with the token in the gitignored `.env`.

Read [API.md](API.md) before touching the account. It records the current API surface, token permissions, documentation links, and safe request pattern.
<!--/ai-->
