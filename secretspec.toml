[project]
name = "calendly"
revision = "1.0"
require_reason = "agents"

[providers]
developer_credentials = "onepassword://Developer-Credentials"

[profiles.default]
CALENDLY_TOKEN = { description = "Calendly personal access token", required = true, providers = ["developer_credentials"] }
